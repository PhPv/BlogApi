/*
package com.jspring1.demo.persistence;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.mongodb.core.MongoTemplate;
import org.springframework.data.mongodb.repository.MongoRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface Rep extends MongoRepository<StackOverflowWebsite, String> {
    List<StackoverflowWebsite> findByWebsite(String website);

//    public List<StackOverflowWebsite> findAll() {
//        return mongoTemplate.findAll(StackOverflowWebsite.class);
//    }
}
*/
